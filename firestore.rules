rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Global read & write access for Karuppa Crackers Store & Admin Portal
    match /{document=**} {
      allow read, write: if true;
    }
    
  }
}
